# VS Code での動かし方（はじめての方向け）

このゲームは **Node.js** という仕組みの上で動きます。手順は次の3ステップだけです。
　1. Node.js を入れる → 2. VS Code でこのフォルダを開く → 3. 起動する

---

## 1. Node.js をインストールする（最初の1回だけ）

1. ブラウザで <https://nodejs.org/ja> を開く
2. 左側の「**LTS**（推奨版）」のボタンを押してダウンロード
3. ダウンロードしたファイルを開き、画面の指示通り「次へ」を押してインストール

インストールできたか確認するには、VS Code を開いて（次の手順2）、
メニューの **［ターミナル］→［新しいターミナル］** を開き、次を入力して Enter:

```
node -v
```

`v22.x.x` のようにバージョンが表示されればOKです。
（`command not found` と出たら Node.js が未インストールか、PC の再起動が必要です）

---

## 2. VS Code でこのフォルダを開く

1. VS Code を起動
2. メニューの **［ファイル］→［フォルダーを開く...］**（File → Open Folder）
3. この `card-game` フォルダを選んで「開く」
   - 「このフォルダー内のファイルの作成者を信頼しますか?」と聞かれたら **［はい、作成者を信頼します］** を選択

左側に `server.js` や `public` などが並んでいれば成功です。

---

## 3. 起動する（毎回これ）

### いちばん簡単な方法：F5 キー
1. VS Code でこのフォルダを開いた状態で **F5 キー** を押す
2. 下に黒い画面（ターミナル）が出て `カードゲームサーバー起動: http://localhost:3000` と表示される
3. ブラウザで **<http://localhost:3000>** を開く

> うまくいかない時は次の「ターミナルから起動」を試してください。

### ターミナルから起動する方法
1. メニュー **［ターミナル］→［新しいターミナル］**
2. 出てきた入力欄に次を打って Enter:
   ```
   node server.js
   ```
3. ブラウザで **<http://localhost:3000>** を開く

### 止め方
ターミナルの中をクリックして **Ctrl + C**（Mac も Control + C）。

---

## スマホで遊ぶ（同じ Wi-Fi 内）

1. PC とスマホを**同じ Wi-Fi** につなぐ
2. PC の IP アドレスを調べる
   - Windows: ターミナルで `ipconfig` → 「IPv4 アドレス」（例 `192.168.1.5`）
   - Mac: ターミナルで `ipconfig getifaddr en0`
3. スマホのブラウザで **`http://（その IP）:3000`**（例 `http://192.168.1.5:3000`）を開く

> つながらない場合は PC のファイアウォールが Node.js / ポート 3000 をブロックしている可能性があります。
> 一時的に許可するか、ファイアウォール設定で Node.js を許可してください。

### 友達と対戦するには
インターネット越し（別の家など）で対戦するには、このサーバーを公開する必要があります。
手軽な方法として、PC で次を実行すると一時的な公開 URL がもらえます（要 Node.js）:

```
npx localtunnel --port 3000
```

表示された `https://～.loca.lt` の URL を友達と共有すれば、同じ「パスワード」で部屋に入って対戦できます。
（本格運用するならレンタルサーバーや Render / Railway などへの配置がおすすめです）

---

## 困ったとき

- **画面が真っ白** … ブラウザを再読み込み（F5）。それでもだめならターミナルのエラー文を確認。
- **`EADDRINUSE`（ポート使用中）** … すでに起動済みです。古い方を Ctrl + C で止めるか、`PORT=3001 node server.js` で別ポート起動。
- **保存に失敗する** … カード画像が大きすぎる可能性。別の（小さめの）画像を選び直してください。
